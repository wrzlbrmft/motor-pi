{
  "name": "client-pi",
  "version": "0.0.0",
  "dependencies": {
    "onoff": "*",
    "socket.io-client": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/onoff": "*",
    "@types/socket.io-client": "*",
    "typescript": "*"
  },
  "scripts": {
    "prestart": "tsc",
    "start": "node build/start.js",
    "clean": "rm -rf build node_modules package-lock.json"
  }
}
