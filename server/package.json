{
  "name": "server",
  "version": "0.0.0",
  "dependencies": {
    "socket.io": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/socket.io": "*",
    "typescript": "*"
  },
  "scripts": {
    "prestart": "tsc",
    "start": "node build/start.js",
    "clean": "rm -rf build node_modules"
  }
}
